# 방안 뜨기 도안 제작 (Filet Crochet Pattern Maker)

## 이게 뭔가

코바늘 뜨개용 격자 도안을 만드는 웹 도구. 취미 프로젝트.

## 결정된 사항

| 항목 | 결정 | 이유 |
|---|---|---|
| 형태 | 정적 웹앱 (HTML/CSS/JS) | PC·모바일 브라우저 모두 지원. 소파에서 폰으로 도안 보며 뜨기 위함. 서버·설치 불필요 |
| 배포 | GitHub Pages | 무료, URL만으로 어디서든 접속 |
| 도안 방식 | 필레 크로셰 (채움/비움 2색) | 데이터 모델이 단순해 시작하기 좋음. 나중에 다색 그래프 도안으로 확장 가능 |
| 런타임 | 없음 (브라우저만) | Python/conda 환경 불필요 |

## 1단계 범위 — 완료 (배포됨)

https://sojung48.github.io/filet-crochet/

- **격자 캔버스**: 행×열 크기 지정, 탭으로 토글 / 드래그로 이어 칠하기
- **도구**: 그리기, 지우기, 채우기(fill), 전체 비우기, 반전, 실행취소/재실행
- **전체 이동**: 화살표로 한 칸씩. 밖으로 나간 칸은 반대편으로 돌아 들어옴
- **보기 옵션**: 10칸마다 굵은 안내선, 칸별 행/열 번호, 확대·축소
- **도안 읽기**: 행별 지시 텍스트 자동 생성 (1행이 맨 아래, 지그재그 방향)
- **저장**: 편집할 때마다 자동 저장 + JSON 내보내기/가져오기
- **내보내기**: 인쇄, PNG
- **모바일**: 터치 대응, 두 손가락 핀치 확대축소

계획과 달라진 것:

- **사각형 도구 제거** — 쓸 일이 적고 폰 툴바가 빡빡해져서 뺐다.
- **"로컬 저장/불러오기" 버튼 제거** — 편집할 때마다 이미 자동 저장되고
  열 때 자동으로 불러오므로, 버튼이 같은 일을 한 번 더 할 뿐이었다.
- **전체 이동 추가** — 그리다 보면 구도가 치우쳐 전체를 밀고 싶어진다.

## 만들면서 알게 된 것

- **ES 모듈은 `file://`에서 CORS로 차단된다.** 그래서 `js/*.js`를
  `js/bundle.js` 하나로 합쳐 일반 스크립트로 넣는다. `js/*.js`를 고치면
  **반드시 `node build.mjs`로 다시 만들어야** 한다.
- **GitHub Pages는 파일을 10분 캐시한다**(`max-age=600`). HTML과 JS가 서로
  다른 시점 것이 되면 새 화면에 옛 동작이 섞인다. 빌드가 번들 내용의 해시를
  `bundle.js?v=...`로 붙여 막고 있다.
- 로컬 저장은 **기기마다 따로**다. 폰↔PC 이동은 JSON으로 해야 한다.
- **iOS 홈 화면 아이콘은 SVG를 못 읽는다.** `apple-touch-icon`에 SVG를 걸면
  아이콘 대신 페이지를 캡처한 그림이 들어간다. 그래서 `icon-180.png`를
  따로 둔다. 아이콘 그림을 고치려면 `icon.svg`와 `make-icon.mjs`를 함께
  고친 뒤 `node make-icon.mjs icon-180.png`를 실행한다.

## 2단계 — 진행 중: 이미지 로드 & 텍스트 변환

손으로 한 칸씩 찍는 것 말고, **그림과 글자를 넣어 도안을 만드는** 두 통로를
연다. 오른쪽 패널을 탭으로 나누고 각 탭이 한 통로를 맡는다.

### 확정된 설계 판단

| 항목 | 결정 | 이유 |
|---|---|---|
| 패널 구조 | 3탭 (도안 / 이미지 / 텍스트) | 세로로 계속 쌓으면 폰에서 스크롤이 너무 길어진다. 기존 4개 섹션은 '도안' 탭으로 그대로 옮긴다 |
| 이미지 색상 수 | **2색 고정** (밝기 임계값) | PRD는 n색도 물었지만, 지금 `renderer.js`·`reading.js`·`invert()`·상태바·PNG가 전부 `OPEN/FILLED` 2색 전제다. n색은 이것들 + 팔레트 검증까지 연쇄 수정이라 3단계로 미룬다. 필레 크로셰 자체가 2색 기법이기도 하다 |
| 2색의 표현력 보완 | 임계값 슬라이더 + 밝기 반전 + 디더링(점묘) | 색을 늘리지 않고도 명암을 살릴 수 있다 |
| 글꼴 | GalmuriMono9 고정, 글자당 9×9칸 | SIL OFL 1.1. 비트맵 폰트라 격자에 1:1로 떨어진다. 출처·주의점은 아래 "글꼴 출처" 참고 |
| 격자 상한 | `MAX_SIDE = 100` 유지 | 올리면 실행취소 스냅샷 메모리가 함께 커진다. 넘칠 땐 경고로 막는다 |

### 글꼴 출처 — GalmuriMono9

만든이 quiple(김규영). **닌텐도 DS 글꼴에서 영감을 받은 비트맵 글꼴**이고,
한글·영문·가나·한자를 모두 담고 있어 한글 도안에 그대로 쓸 수 있다.

| 항목 | 내용 |
|---|---|
| 공식 저장소 | https://github.com/quiple/galmuri |
| 소개 페이지 | https://galmuri.quiple.dev/ |
| npm | `npm i galmuri` |
| CDN | `https://cdn.jsdelivr.net/npm/galmuri/dist/galmuri.css` |
| 파일 (dist) | `GalmuriMono9.woff2` (402 KB), `.ttf` (4.3 MB), `.bdf` (2.4 MB) |
| 확인한 버전 | v2.40.3 (2026-08 기준 최신) |
| 라이선스 | SIL Open Font License 1.1 — 자유롭게 쓰고 고치고 재배포 가능. **글꼴 자체를 파는 것만 금지** |

**우리는 CDN을 쓰지 않고 `GalmuriMono9.woff2`를 저장소에 직접 넣는다.**
이 앱은 `file://`로 더블클릭해도 동작해야 하고(README·1단계 기록 참고)
GitHub Pages에서 외부 CDN이 막히거나 느려도 글자가 깨지면 안 되기 때문이다.
OFL 1.1은 재배포를 허용하므로 파일을 함께 두어도 된다. 다만 **라이선스 전문을
`fonts/OFL.txt`로 같이 넣어야 한다** — OFL은 사본 동봉을 요구한다.
`.woff2`만 받는다(402 KB). `.ttf`는 4.3 MB라 폰에서 받기에 무겁다.

**주의 — 이름의 "9"가 글리프 상자 크기가 아니다.**
공식 표기로 Galmuri9는 **10px / 7.5pt**다. 즉 `font-size: 9px`로 그리면
글자가 잘리거나 흐려질 수 있다. PRD가 정한 "글자당 9×9칸"이 실제로 맞는지는
**스텝 3에서 캔버스에 찍어 픽셀을 직접 세어 확인한다.** 확인 결과 10×10이
맞다면 계산식의 9를 10으로 바꾸고, 상한(가로 10글자/세로 10줄)도 다시
계산한다(10칸 + 간격 1칸이면 한 줄 9글자, 세로 9줄로 줄어든다).

**Google Fonts에는 없다.** 찾아봤으나 등록되어 있지 않아, 위 GitHub/npm이
유일한 공식 배포처다. 폰트 무료 배포 사이트(fontmeme 등)에도 올라와 있지만
버전이 오래됐을 수 있으므로 공식 저장소에서만 받는다.

### 텍스트의 실제 한계 — 가로·세로 둘 다 본다

```
필요 가로 = (한 줄 글자 수 × 9) + (글자간격 × (글자 수 − 1))
필요 세로 = (줄 수      × 9) + (줄간격   × (줄 수   − 1))
```

(9는 스텝 3에서 실측해 확정한다 — 위 "글꼴 출처"의 10px 주의 참고)

간격 기본값 1칸 기준으로 **가로 10글자 / 세로 10줄**이 상한(각각 99칸)이다.
줄바꿈으로 여러 줄을 쓰면 세로가 먼저 걸리므로 **두 축을 함께 검사**하고,
경고 문구도 가로가 넘쳤는지·세로가 넘쳤는지·둘 다인지 구분해 알려준다.
상한 안이면서 현재 도안보다 크기만 한 경우에는 "최소 N×M이 필요합니다.
도안 크기를 변경하시겠습니까?"로 묻고 확인 시 자동으로 키운다.

### 작업 스텝 — 한 스텝씩 확인받고 진행

| 스텝 | 내용 | 건드리는 곳 |
|---|---|---|
| 1 | 패널 3탭 골격. 기존 섹션을 '도안' 탭으로 이동, 나머지는 빈 껍데기. 탭 전환·모바일 대응·마지막 탭 기억 | `index.html`, `css/style.css`, `js/main.js` |
| 2 | 이미지 탭: 파일 로드 → 격자 크기로 리샘플 → 임계값·반전·디더링 미리보기 → 도안 적용 → 🗑 비우기 | `js/image.js` (신규) |
| 3 | GalmuriMono9 `.woff2` + `OFL.txt`를 `fonts/`에 넣고, 글리프를 캔버스에 찍어 **실제 칸 수(9인지 10인지)와 한글 렌더를 실측** | `fonts/`, 검증용 페이지 |
| 4 | 텍스트 탭: 입력·간격 UI → 필요 크기 계산 → 상한 경고 / 크기 변경 확인 → 도안 적용 | `js/text.js` (신규) |

신규 모듈은 `build.mjs`의 `FILES` 배열에 **의존 순서대로** 넣어야 한다
(`main.js`는 항상 마지막).

### 스텝별 메모

- **원본 이미지는 세션 중에만 메모리에 남긴다.** 새로고침하면 사라지고,
  🗑 비우기로도 지운다. localStorage에 넣지 않는다 — 도안 하나가 몇 MB가
  되어 자동 저장이 통째로 실패할 수 있다.
- **도안 크기를 바꾸면 원본에서 다시 변환한다.** 이미 격자로 줄인 결과를
  또 늘리면 계단이 생긴다.
- 이미지 파일도 JSON과 같은 외부 입력이다. **크기 상한을 이때 같이 넣는다**
  (아래 "미뤄둔 것"의 파일 크기 제한 항목이 여기서 실제 문제가 된다).

## 3단계 (나중에)

- PWA (홈 화면 추가, 오프라인 사용)
- 다색 그래프 도안 / C2C 방식 확장 — 이미지 n색 변환도 여기에 딸려온다
  - **다색으로 넘어갈 때 팔레트 입력 검증을 반드시 같이 할 것.**
    지금은 JSON 가져오기가 `palette`를 검사 없이 그대로 받는다. 이름에
    `<img src=x onerror=...>` 같은 값이 들어와도 저장되고 재내보내기까지
    살아남는다. 2색인 지금은 팔레트를 화면에 표시하지 않아 실제 피해가
    없지만, 팔레트 이름·색을 UI에 쓰는 순간 그대로 XSS가 된다.
    - `Pattern.fromJSON`에서 팔레트를 정규화: 이름은 태그 문자 제거 후
      길이 제한, `css`는 `#rrggbb` / 색 이름 / `rgb()` 등 안전한 형태만 허용
    - 항목 개수 상한도 둘 것 (거대 팔레트로 localStorage를 채우는 것 방지)
  - 함께 고쳐야 하는 곳: `renderer.js`(칸 색), `reading.js`(색별 지시문),
    `Pattern.invert()`(2색 전제), 상태바 "채움 N", PNG 내보내기 흑백 고정

## 미뤄둔 것

급하지 않아 넘겼지만 잊으면 안 되는 것들. JSON 가져오기가 유일한 외부
입력 통로라 손보려면 `Pattern.fromJSON`과 `storage.js` 두 곳만 보면 된다.

| 항목 | 지금 상태 | 언제 필요한가 |
|---|---|---|
| 팔레트 검증 | 검사 없이 통째로 수용 | 다색 확장 시 (위 3단계 항목 참고) |
| 가져올 파일 크기 제한 | 없음 — 수백 MB 파일을 고르면 탭이 멈춤 | **2단계 스텝 2에서 처리한다.** 사진은 JSON보다 훨씬 크고, 폰 사진첩에서 고르는 일이 잦다 |
| 저장 공간 초과 처리 | 실패 시 토스트만 띄우고 넘어감 | 도안이 커지거나 저장 실패를 겪으면 |

취미용 로컬 도구이고 도안 파일을 아무 데서나 받아오지 않는 한 실제 위험은
낮다. 다만 친구와 파일을 주고받기 시작했으므로 여지는 있다.

## 확장 여지

데이터 모델은 처음부터 "칸 = 값" 구조로 두되, 값이 boolean이 아니라 팔레트 인덱스가 될 수 있게 설계할 것. 그래야 2색 → 다색 확장 시 저장 포맷을 갈아엎지 않아도 됨.
